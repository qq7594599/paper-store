## Build with iOS platform

```shell
$ ionic cordova build ios --buildFlag="-UseModernBuildSystem=0"
```

## Update icon

``` shell
$ ionic cordova resources
```

## Android Studio
Need remove `uses-sdk` section in Manifest.xml